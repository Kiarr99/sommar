#Merge sort
#Kiar Fatah
import numpy as np


#help_func is needed to call is_list_sorted
def help_func(arr):
    if len(arr) = 1:
        return True
    else:
        r = len(arr)
        return is_list_sorted(mergesort(arr,l = 0,r))
    


#Check if the list is sorted for testing
def is_list_sorted():


def mergesort(arr,l,r):
    while 



#Test
if __name__ == "__main__":

for i in range(1,100):
    assert help_func(np.random.rand(i,1)) == True
